import React from "react";

const Header = () => {
  const styles = {
    textAlign: "center",
    fontSize: "3em",
    color: "#fff",
  };
  return ( 
     <div style={styles}>
        Personal Roadmap
    </div>
    )
};

export default Header;